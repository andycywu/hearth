{
  "name": "@hearthkit/skills-example",
  "version": "0.2.0",
  "private": true,
  "description": "Worked example of a cross-vendor skill: a keyless weather tool that runs identically on every TV target because it needs no device privilege. See docs/skills.md.",
  "type": "module",
  "sideEffects": false,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -b",
    "typecheck": "tsc --noEmit",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "@hearthkit/core": "workspace:*"
  },
  "license": "Apache-2.0"
}
