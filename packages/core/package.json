{
  "name": "@tv-ai-agent/core",
  "version": "0.1.0",
  "description": "Platform-agnostic on-device AI agent runtime: agent loop, tool registry, LLM client abstraction, context/memory and event bus.",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -b",
    "typecheck": "tsc --noEmit",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "@tv-ai-agent/platform-api": "workspace:*"
  },
  "devDependencies": {
    "@tv-ai-agent/adapter-web": "workspace:*"
  },
  "license": "Apache-2.0"
}
