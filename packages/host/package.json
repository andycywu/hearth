{
  "name": "@hearthkit/host",
  "version": "0.1.0",
  "description": "One boot sequence for every device host: adapter, model, room, planner, shell. The per-platform entries supply only what actually differs.",
  "type": "module",
  "sideEffects": false,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -b",
    "typecheck": "tsc --noEmit",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "@hearthkit/core": "workspace:*",
    "@hearthkit/llm-connectors": "workspace:*",
    "@hearthkit/modelpilot": "workspace:*",
    "@hearthkit/platform-api": "workspace:*",
    "@hearthkit/ui": "workspace:*"
  },
  "license": "Apache-2.0"
}
