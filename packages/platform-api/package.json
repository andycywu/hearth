{
  "name": "@tv-ai-agent/platform-api",
  "version": "0.1.0",
  "description": "Platform abstraction layer (HAL) — capability interfaces the agent uses to control a TV, independent of AOSP/Tizen or MTK/NVT.",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -b",
    "typecheck": "tsc --noEmit",
    "test": "vitest run --passWithNoTests"
  },
  "license": "Apache-2.0"
}
