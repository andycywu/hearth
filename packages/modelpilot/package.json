{
  "name": "@hearthkit/modelpilot",
  "version": "0.1.0",
  "description": "ModelPilot Execution Decision Engine adapter: routes planning and reasoning through ModelPilot while device control stays local and locally verified.",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -b",
    "typecheck": "tsc --noEmit",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": { "@hearthkit/core": "workspace:*" },
  "devDependencies": { "@hearthkit/adapter-web": "workspace:*", "@hearthkit/platform-api": "workspace:*" },
  "license": "Apache-2.0"
}
