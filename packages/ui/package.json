{
  "name": "@hearthkit/ui",
  "version": "0.2.0",
  "description": "Dependency-free 10-foot UI shell for the agent (DOM). A Lightning 3 / Blits WebGL renderer can replace the view layer for low-end MTK/NVT GPUs; see README.",
  "type": "module",
  "sideEffects": false,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -b",
    "typecheck": "tsc --noEmit",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "@hearthkit/core": "workspace:*",
    "@hearthkit/platform-api": "workspace:*"
  },
  "license": "Apache-2.0"
}
