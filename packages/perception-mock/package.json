{
  "name": "@tv-ai-agent/perception-mock",
  "version": "0.1.0",
  "description": "Scripted perception sources — occupancy, ambient light, noise — with no camera and no CV model. Proves the perception path end to end.",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -b",
    "typecheck": "tsc --noEmit",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": { "@tv-ai-agent/core": "workspace:*" },
  "devDependencies": { "@tv-ai-agent/adapter-web": "workspace:*" },
  "license": "Apache-2.0"
}
