{
  "name": "@hearthkit/perception-mock",
  "version": "0.1.0",
  "description": "Scripted perception sources — occupancy, ambient light, noise — with no camera and no CV model. Proves the perception path end to end.",
  "type": "module",
  "sideEffects": false,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -b",
    "typecheck": "tsc --noEmit",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "@hearthkit/core": "workspace:*"
  },
  "devDependencies": {
    "@hearthkit/adapter-web": "workspace:*"
  },
  "license": "Apache-2.0"
}
