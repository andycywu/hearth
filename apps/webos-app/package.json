{
  "name": "@hearthkit/webos-web",
  "version": "0.1.0",
  "private": true,
  "description": "webOS (LG) web-app host — bundles the agent runtime into an .ipk for webOS TVs.",
  "scripts": {
    "build": "node ../../tools/bundle.mjs webos",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "@hearthkit/host": "workspace:*",
    "@hearthkit/adapter-webos": "workspace:*"
  },
  "license": "Apache-2.0"
}
