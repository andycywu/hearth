# Project type [web_app, test_runner]
project_type: web_app

# Default profile, Tizen API version
profile: tv
api_version: "6.0"

# Build type [Debug/ Release/ Test]
build_type: Debug

# Output name for application
output_name: TvAiAgent

# Output path for build
output_path: ""

# Enable size optimization of wgt for web workspace
opt: false

# Signing profile to be used for Tizen package signing
# If value is empty: "", active signing profile will be used
# Else If value is ".", default signing profile will be used
signing_profile: ""

# list of certs in web project (.trust-anchor)
trust_anchor: []

# list of files in web project (.html,.css etc) and resources
# Only what the app needs at runtime: the TypeScript source and package.json are
# build inputs, and shipping them would just bloat the .wgt.
files:
  - config.xml
  - icon.png
  - index.html
  - main.js
  - style.css

# list of files to exclude based on the matched patterns
excludes:
  # pnpm links workspace deps as junctions; the packager can't read them and
  # fails with "Incorrect function". They're build-time only in any case.
  - node_modules/*
  - node_modules/.*
  - main.js.map
  - src/*
  - package.json
  - README.md
  - Build/*
  - Debug/*
  - Directory.Build.targets
  - Release/*
  - Test/*
  - \.Build/.*
  - \.buildResult/.*
  - \.csproj
  - \.externalToolBuilders/.*
  - \.gn
  - \.obj/.*
  - \.package/.*
  - \.project
  - \.rds_delta
  - \.sdk_delta.info
  - \.settings/.*
  - \.sign/.*
  - \.tizen-ui-builder-tool.xml
  - \.tproject
  - \.wgt
  - tizen_web_project.yaml
  - webUnitTest/*

# project dependencies
deps: []

